import 'package:flutter/material.dart';

class Addtsk extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Add Task")),
    );
  }
}